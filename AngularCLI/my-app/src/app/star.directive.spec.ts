import { StarDirective } from './star.directive';

describe('StarDirective', () => {
  it('should create an instance', () => {
    const directive = new StarDirective();
    expect(directive).toBeTruthy();
  });
});
