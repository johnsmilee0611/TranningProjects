import { Star2Directive } from './star2.directive';

describe('Star2Directive', () => {
  it('should create an instance', () => {
    const directive = new Star2Directive();
    expect(directive).toBeTruthy();
  });
});
